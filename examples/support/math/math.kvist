(package math)
(import arr "kvist:arr")

(defn sum-range
  "Return the sum of integers from START up to END."
  [start: int, end: int] -> int
  (let [sum 0]
    (each [i (arr.range start end)]
      (set! sum (+ sum i)))
    sum))
