// Copyright (c) Andreas Flakstad and Kvist contributors
// SPDX-License-Identifier: MIT

(package order-independent)

(defmacro- emit-box [n]
  (quasiquote (Box {value: (unquote n)})))

(defmacro make-box [n]
  (emit-box n))
