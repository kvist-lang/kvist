// Close port of the Cluck reset docs demo.
(package main)
(import core "kvist:core")
(def docs_banner
  "Banner used by the docs demo."
  "doc demo")

(defn docs-demo
  "Show doc lookup for both value and function declarations."
  [] -> bool
  (println docs_banner)
  (doc 'docs_banner)
  (doc 'docs_demo)
  true)

(defn main []
  (println (docs-demo)))

(comment
  (docs-demo))
