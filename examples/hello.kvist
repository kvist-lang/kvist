// Greets from Kvist.
(package main)
(import "core:fmt")

;; This is a greeting
(struct Greeting {
  :message string
})

(proc main []
  (let [g (Greeting { :message "hello from kvist" })]
    (fmt.println (:message g))))

(comment

  (-> (Greeting { :message "hello!" })
      :message
      len)
  ;; => 6

)
