(package hybrid_live_demo_module)

(import hot "kvist:hot")
(import kvist_hot "../../../../src/kvist_hot")
(import shared "../shared")

(def hybrid-demo-version: string "native module v1")

(hot.defmodule shared.State
                hybrid-demo-message
                hybrid-demo-tick
                hybrid-demo-version
                "native module v1 loaded")
