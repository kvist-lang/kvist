// Greets from OdinL.
(package main)
(import "core:fmt")

(struct Greeting {
  :message string
})

(proc main []
  (let [g (Greeting {
            :message "hello from odinl"
          })]
    (fmt.println (:message g))))
